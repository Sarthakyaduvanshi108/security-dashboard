# security-dashboard
A Security Dashboard UI built with React and Vite. Includes login authentication form, dashboard with scan results, and detailed vulnerability findings. Features sidebar navigation, reusable components (StatusChip, SeverityBadge, ProgressTracker), and mock data integration for testing.
